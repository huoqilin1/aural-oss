## 2026-09-15 当前发布授权与验收定案

用户本轮明确要求：只验收10路，永久停止20路测试；20路历史失败不构成发布阻塞。已通过的完整八题真实报告、HR四维评分及10路结果仍有效。用户已授权本次已验收修复的提交、推送和部署，无须重复询问；按同版制品发布流程执行。此授权不取消验收，也不授权改写历史失败为通过。上线后继续检查HR简历姓名识别及模型切换问题。

# 2026-09-15 本地续验结果

本轮没有推送、部署或修改生产服务。完整报告链路仅复核既有证据，没有重跑；HR 四维评分使用该真实页面面试的归档输入，在独立数据库副本执行。

| 验收层 | 结果 | 实际证据 |
| --- | --- | --- |
| 候选人页面八题 → 真实 GLM 最终报告 → 落库/刷新 → HR 对账 | 已确认通过 | 原版本收据 8/8 文件哈希仍匹配，原 BUILD_ID KA-2FzJMWMHwxgYXU849J 保持不变；本轮没有重新生成报告 |
| HR 四维评分 worker → 真实 GLM → 评分落库并复读 | 通过 | 97.2 秒；processed=1/scored=1/pending=0；四维分数与题号证据有效；selection_report_status=complete、sync_status=synced；回答未变、锁释放 |
| 同环境 10 路完整页面行为模拟 | 通过 | 10/10 完成八题、每场一次录音保存；测试约29.1秒、进程退出0；动作 trace 有124个操作；摄像头、浏览器录音与原超时保留 |
| 历史20路诊断（已取消） | 仅留档，不再测试、不阻塞上线 | 多组对照均未达到20/20；最后低干扰轮次20路就绪、0路通过、20路失败；动作 trace 有14个操作。不能说20路容量通过 |
| 本轮变更的静态检查 | 通过既有基线 | TypeScript 46/51、ESLint 21/21，无新增诊断；不是零历史问题 |

## 20 路目前能确认什么

- 失败已复现于完整本地 Next 页面。最后的低干扰轮次与10路对照使用相同原产品代码、正式构建、系统Chrome153、每浏览器5个独立上下文；保留动作/网络trace，不启用CPU采样或DOM快照。
- 10路最大页面调度延迟约0.60秒；20路约24.82秒。20路最低可用内存仍为9487MB，因此不能笼统说“内存不够”。失败位于页面就绪/按钮操作/录音完成等阶段。
- 真实模型调用为0，录音上传/保存与中转端点为受控模拟。这些结果不证明模型或后端的20路吞吐，也不能反推原手机截图的唯一根因。
- 开发/正式构建、单/多浏览器、系统/配套浏览器、音频静音输出和采样开销对照均有记录。首次静音对照缺少生效校验、软件GPU轮次存在资源和清理干扰，明确不作为有效因果证明；后续已验证静音输出轮次也失败。
- CPU记录发现产品scrollIntoView热点，但限制滚动容器后的20路验证失败，因此该产品尝试已全部撤回。最终voice-interface.tsx与原基线无差异。不能把热点当成已修好的根因。
- 当前证据支持“这台宿主承载20个页面时存在显著的浏览器页面调度瓶颈”；其底层唯一原因和可复现的有效修复仍未证实。不能宣称整体全部通过或据此上线。

## 证据与版本

- 主检查点：`docs/LOCAL_ACCEPTANCE_ACTIVE_CHECKPOINT_2026-09-14.md`。
- 原报告收据：`output/live-report-version-receipt-20260915.json`；原会话 `0b026a89-a7d9-472b-84bf-981182a041ad`。
- HR评分：`output/hr-score-acceptance-1789441638283801400/receipt.json`、`score.json`；执行器 `scripts/accept-live-hr-score.py`。使用独立SQLite和内存Redis降级，不冒称分布式数据库或HR浏览器UI验收。
- 所有并发轮次索引、失败详情、浏览器指标和trace校验：`output/concurrency20-diagnostic-receipt-20260915.json`。
- trace/CPU原始证据保留在 `output/concurrency-traces-20260915/`；原日志路径保持历史原样，索引另列当前有效路径。
- 完整测试源码副本已移动到 `D:\GGGG\kiro\aural-concurrency20-local-20260915`，避免主项目类型检查递归扫描副本。原报告`.next`未被替换。临时3300服务及测试进程已结束，未关闭宿主原先的报告浏览器或其他用户服务。
- Aural基线 `d176cdb7fba414f4d7ba9a17d55389cc9545618b`；HR基线 `801f7f2ae36f1369aafc21b7873cbbff52877e9e`。本轮最终保留诊断脚本、验收记录和测试观测开关，没有新增产品改动。

当前结论（按用户最新更正）：报告链路、HR四维评分和10路本地行为通过。20路已取消，不再作为验收或发布阻塞。用户已授权本次已验收修复上线，发布结果另行记录。
