# 三项要求的实际执行结果

## 审批

用户要求取消人工审批、由模型自动执行。本任务宿主提供的有效审批模式已为 `never`，不等待人工审批。用户配置文件没有显式的 `approval_policy` / `approvals_reviewer` 字段；不能因此推定改写用户配置会覆盖当前任务的宿主设置。本轮未改写安全配置，未将模型自动推进理解为可以覆盖策略拒绝。

## 完整页面、真实模型、最终报告

复测原 Next 启动命令（原目录、原端口 3219、仅本机测试占位配置），执行工具在 CreateProcess 前返回 `blocked by policy`。没有创建 Next 进程，没有实际完成页面到模型及报告链路。没有通过替换工具或启动器绕过拒绝；没有将已有组件、直接路由函数和测试报告接收器算作完整链路。

## iPhone 猎聘模拟

执行 `node --import tsx --test --test-concurrency=1 tests/recruitment-media-access.test.ts tests/recruitment-entry-browser.test.ts`，显式使用合成 iPhone / Liepin UA、390×844 屏幕、移动与触摸设置。UA 是模拟参数，不是从真实猎聘 App 提取的证据。实际引擎 Chromium 145.0.7632.6。

| 检查 | 结果 |
|---|---|
| 一次开始自动申请音视频并进入 Q1 | 通过 |
| 权限等待及迟到授权自动进入 | 通过 |
| 拒绝后持续提示、恢复权限自动进入 | 通过 |
| 陈旧请求与较晚回调的竞态 | 通过 |
| 缺设备提示、退出后的资源回收 | 通过 |
| 前后台恢复信号不重复申请或建连接 | 通过 |
| 缺 Permissions 查询 API 的恢复 | 通过 |
| 中转连接失败、online 后恢复 | 通过 |
| 单元资源竞态与生命周期 | 通过 |

合计 10 个组件浏览器用例和 6 个单元用例，16/16、0 跳过、退出 0。不是 16 次完整面试。证据：`output/liepin-simulation-20260914-211458/run.log`、`source-hashes.json` 及截图。人工视觉复核（代理读取截图）确认 pending 提示在手机首屏，计时保持 0:00，题目保留，没有新增开始按钮。

测试工具会话 `57445` 已结束，浏览器由 after 关闭。本轮只新增测试证据输出目录参数和环境标识日志，产品源码没有改变。未反复运行不受影响的生产构建。

**边界：** 此结果不验证真实 iOS WebKit 或猎聘内嵌浏览器策略，也不能唯一归因历史截图。完整页面、真实模型和最终报告仍未完成。本次没有提交、推送或部署，仍未达到上线标准。
