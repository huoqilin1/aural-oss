# 2026-09-06 第二题误判与保存顺序修复记录

## 范围与状态

本轮只修复上一份生产模拟面试暴露的问题，不开展新的生产投递批次，不做全面系统审计。
当前为本地未提交修改；未推送、未部署，不能称为生产面试验收通过。

- Aural 工作区：`D:/GGGG/kiro/aural-access-release`，基线 `7c7ec9325b47051345005733edee7cd79bd90dfa`。
- HR 工作区：`D:/GGGG/kiro/oprun-hr-main-wt`，基线 `34b34ae4ca44a45caa838439b5ec196008f02b09`。
- HR 主工作区其他未提交业务修改未纳入本次修复。
- 上一份生产失败记录：`D:/GGGG/kiro/oprun-hr/.artifacts/production/acceptance-7c7ec93/RESULT.md`。

## 问题、处理与验收证据

| 问题 | 本地处理 | 检查方式 |
|---|---|---|
| “我不会自行承诺”等正常否定句被识别为跳题，真实回答被拒绝 | 招聘流程不再使用宽泛的通用跳题关键词；区分正常回答、回答完毕和结束整场面试 | 使用合成业务句复现，执行实际主通道处理函数；验证否定句、缺乏经验、引用命令、正文追加“我答完了” |
| 切题先于答案落库 | 主备通道先请求浏览器保存，只有匹配请求和题号的成功确认才能前进 | 延迟、失败、超时、错误题号、过期确认、断连测试；执行实际切题函数 |
| 上一笔保存失败后，后一笔空保存可能被当成保存成功 | 在串行保存任务真正执行时取消息，包含前序失败退回的回答 | 执行实际浏览器保存回调，第一笔失败且第二笔排队，确认第二笔包含原回答 |
| 备用模型工具调用等待保存时，“回复结束”抢先处理 | 同一模型连接的事件按序处理，浏览器保存确认独立接收；切题锁阻止并发工具推进 | 执行实际备用连接消息处理器，提前发送 response.done，分别模拟保存成功和失败 |
| 拒绝后持续“思考”，候选人无法继续回答 | 拒绝时清理 ASR 思考计时，恢复听答；识别中不冒充模型思考 | 本地浏览器保存失败后仍在第二题、显示可听答并可重试 |
| 重复 ASR 完毕命令干扰状态 | 按题号、已收回答和命令意图去重；保存失败后清除已消费控制标记，允许重试 | 原有迟到 ASR 浏览器测试；实际切题失败测试确认重试标记释放 |
| Q2 含条目编号、机械念题和任意字数截断 | HR 按完整源语句/标点边界提取锚点；去掉条目编号；主通道不额外念题号 | HR 题干行为测试验证原文锚点、括号完整、不截半个词；保留简历与岗位双锚 |

保存确认超时仅表示此次保存未确认：保留本题及回答、恢复听答，允许重试；不是面试时长上限，不结束面试。

## 不变的产品要求

阅读须知 → 一次开始面试 → 自动连接摄像头和麦克风。摄像头保持开启，不增加设备测试。
Q1 计分；有效邀请及 Q1/Q2 就绪即开放，不等待 Q3–Q8；八题完整性、个性化、真实性证据边界、有限追问、无 32 分钟强停均未改变。
不因正常否定句扣除一条真实回答，也不把“没有相关经验”当作撒谎。

前一轮已获单项批准的简历规则一并保留：可使用获授权原件或已有脱敏副本，不强制制作脱敏文件。
生产批次和数量授权、测试标识、不通知本人及日志隐私要求仍有效。

## 本地验证账本

| 检查 | 结果 | 边界 |
|---|---|---|
| `npm run test:web` | 318/318 通过 | 包含实际嵌套函数配隔离 I/O 的行为测试；无真实模型/生产数据库调用 |
| `node --test tests/resume-authorization-policy.test.mjs` | 5/5 通过 | 原件/已有副本均允许；缺少批次、样本指纹、目标授权仍阻止 |
| `npm run test:functional` | 最终修改版本 19/19 通过 | 真浏览器、模拟媒体/中转/保存接口；不是生产语音全链路证明 |
| HR 四组 pytest | 76/76 通过 | 产品合同、可靠性、实时同步、生命周期；存在 3 条已有 SQLAlchemy 弃用警告 |
| `npm run lint:ratchet` | 21/21 已知诊断，无新增 | 不是零 lint 历史问题 |
| `npm run typecheck:ratchet` | 46/51 基线诊断，无新增 | 不是全库零 TypeScript 错误 |
| `git diff --check` | 通过 | Aural、HR 范围差异检查 |
| 清除旧构建影响后的本地 `npm run build` | 通过，退出码 0 | 使用占位配置；不是生产发布制品 |

浏览器回归在补齐顺序修复后重新执行，不沿用修复前的通过结果。
既有浏览器夹具的缺失本地会话请求会输出模拟保存接口错误；本轮故障注入的 503 属于预期异常路径，不对应生产请求。
构建前将旧 `.next` 移至本工作区 `.artifacts/local-negation-next-before-build` 保留，重新生成，不复用旧页面。
本地构建 ID：`paIrxIHNr3aFt6Vu-2aKX`。项目既有构建配置跳过构建内 lint/type 校验，故本轮另行执行两项 ratchet 门禁，未修改基线；构建后再次运行类型门禁通过。构建存在 Browserslist 数据过期和缓存体积提示，未自动升级依赖。

本地日志在 Aural `.artifacts/local-negation-{unit,browser,lint,types,build}.txt`，HR `.artifacts/local-negation-hr.txt`。

## 后续发布与最终验收

1. 用户批准本次 HR/Aural 提交并推送 main 后，审查精确暂存差异；不得夹带其他任务改动。
2. 推送后记录两个明确 SHA，单独获得部署批准，使用干净远程 main 构建不可变制品。
3. 验证版本、服务和入口；这些检查不能代替完整面试。
4. 单独确认最终生产批次后，先跑 1 份获授权简历，从官网进入，重点覆盖正常否定回答、回答完毕、追问、八题、断连恢复和最终保存。
5. 对账 HR/Aural 八题回答、完成状态、报告、四维评分与证据。AI 模拟只验证产品，不评价简历本人的真实性或能力。
6. 若失败，重开问题；先修完和本地验证，再安排新获批最终批次，不循环投递调试。

当前未证明：修复后的生产语音全链路、多样本质量及 20 路活动语音并发。不得据本地通过宣称这些已完成。
