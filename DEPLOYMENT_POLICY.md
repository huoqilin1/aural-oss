# 合并、推送与部署强制规则

本文件是本项目所有开发助手、脚本和人工操作的必读规则。每次开始修改、合并、推送、部署前，都必须重新读取本文件，不得依赖记忆。

## 三条最高优先级红线

1. **未经王总在当前任务中明确同意，禁止部署或重启生产服务。**“执行修复”“继续处理”“测试一下”不等于同意部署；必须出现明确的“同意部署/可以上线”等授权。
2. **未经王总在当前任务中明确同意，禁止合并或直接推送到 `main`。**允许在功能分支本地修改、测试和提交，但不能据此推定有权操作 `main`。
3. **生产只能部署 Git 中已经进入远程 `main` 的明确提交。**禁止从工作区、功能分支、未提交文件、服务器临时修改或其他 ref 部署；禁止 SCP/rsync 绕过 Git 上线。

## 固定执行顺序

`规则与代码审计 -> 可执行正常/异常路径测试 -> 干净生产构建 -> 报告本地验收结果 -> 王总明确同意合并 main -> 合并并推送 main -> 王总明确同意部署 -> 从远程 main 的指定提交部署 -> 版本/服务/API/浏览器验收 -> 另行获批的脱敏真实简历生产真人面试 -> HR/Aural 数据与报告对账 -> 本次范围内的多样本和并发验收`

合并授权和部署授权是两个独立授权。只同意合并，不代表同意部署；只说修复，也不代表两者中的任何一个。

## Hook 授权参数

得到明确授权后，操作者仍须为 hook 提供与本次动作匹配的参数：

- 合并到 main：`OPRUN_USER_APPROVED=YES` 且 `OPRUN_APPROVED_ACTION=merge-main`
- 推送 main：`OPRUN_USER_APPROVED=YES` 且 `OPRUN_APPROVED_SHA` 必须等于待推送提交 SHA
- 服务端接收 main：推送必须携带 `user-approved=<待推送提交SHA>` push option

这些参数只是技术门禁，不能代替王总的真实授权。没有当前任务中的明确同意，任何人或 AI 都不得自行设置参数绕过门禁。

## 模拟检测边界

本地单元测试、编译、静态检查、使用合成数据调用无写入分析接口属于检测，不构成部署。涉及真实候选人数据写入、服务重启、服务器代码替换、数据库迁移、远程 main 更新的操作均不属于检测，必须单独获得明确同意。

## 招聘发布验收铁律

- 静态源码检查、自动化行为测试、构建、部署健康检查、生产真人面试、多样本和并发分别记账；
  同一种检查重复多次不得冒充不同层级。
- 自动化必须覆盖正常路径及静默、无回答推进、提前完成、抢话/文字竞态、断线重连、主备
  中转、动态扩题、保存同步失败、八题完整性、追问预算和自动音视频入口。
- 生产验收必须从官网真实投递开始，完成语音面试后核对 HR 与 Aural 的八题回答、追问数、
  完成状态、四维评分和证据引用；生产写入和所用脱敏文件仍须单独获得明确授权。
- 健康检查通过只能报告“已部署，尚未完成生产验收”；单样本通过只能报告“单样本生产验收
  通过”。只有当前范围内所有门槛通过才可报告“全部完成”。
- 代码、配置、制品或部署 SHA 变化后，受影响的下游结果一律作废；任一失败必须修复并从
  受影响层重新验收，禁止复用旧通过结果。
