## 2026-09-16 用户澄清：本机验收数量不是系统容量上限

本机资源有限，本次并发验收以10路为准，不要求20路全部通过，也不继续在本机运行20路。系统及测试工具仍支持20路或更多；不得把验收数量写成产品或脚本硬上限。此前“永久停止20路”“只允许10路”的表述是助手误解，现明确撤销。历史结果保留，不冒称已验证20路容量。

模型策略：隔离测试使用 RECRUIT_MODEL_MODE=test，锁定GLM；正式使用 RECRUIT_MODEL_MODE=production，支持HR手动选择与失败自动切换备用。测试锁不得带入正式策略。

## 2026-09-15 当前发布授权与验收定案

用户本轮明确要求：本机只验收10路，不把20路全通过设为门槛；20路历史失败不构成发布阻塞。已通过的完整八题真实报告、HR四维评分及10路结果仍有效。用户已授权本次已验收修复的提交、推送和部署，无须重复询问；按同版制品发布流程执行。此授权不取消验收，也不授权改写历史失败为通过。上线后继续检查HR简历姓名识别及模型切换问题。

## 2026-09-15 准则定案：并发验收固定为 10 路（王总最新指令，最高优先级）

王总明确决定：本次本机并发验收为10路，不要求本机20路通过；系统及工具不得限制为10路。此前的 20 路 trace 诊断轮次已结束，其失败结论仅作历史留档，不构成上线阻塞，也不再要求继续定位 20 路根因。完整页面到真实模型、最终报告落库及对账的验收要求不变；iPhone 猎聘真机门槛保持取消。禁止推送和部署，须另行明确授权。
## 2026-09-15 用户更正：部署授权不取消验收（最高优先级）

用户明确指出：授权部署不等于取消完整报告链路验收。此前助手把授权解释为不再验收，并写成“本次豁免”，属于助手错误解读，不是用户要求。完整页面完成面试、真实模型生成最终报告、报告落库以及 HR 收到并对账仍须继续执行，不能以部署成功结束任务。并发仍仅 10 路，不恢复 iPhone 猎聘真机门槛。

以下历史记录中有关“用户明确豁免未验收项”的表述均由本更正覆盖；保留失败和执行记录，但不得继续引用为停止验收的依据。
## 2026-09-15 本次发布授权与例外（仅本次）

用户在获知完整页面至真实模型、最终报告落库及对账尚未完成后，明确回复同意并完全授权执行部署。本次授权涵盖当前入口媒体修复、报告保存确认修复所需的提交、合并 main、推送和不可变制品部署，无须重复询问。

本次豁免未完成的完整报告链路上线前门槛，不能记作该链路通过；后续修改仍执行本地优先要求。并发仅 10 路，iPhone 猎聘真机不作为门槛。生产仅做限定版本、服务、接口与公开页面检查，不向候选人发送测试通知。部署结果另行记录。
# 合并、推送与部署强制规则

## 当前任务验收范围（王总 2026-09-15 更正，优先于本文旧范围）

- 本机并发验收为10路，不要求本机运行20路，不限制系统容量或工具能力；历史 20 路失败保留为历史，不再作为上线阻塞。
- 取消 iPhone 猎聘真机验收及上线门槛，保留本地手机模拟，不得冒称真机通过。
- 完整页面到真实模型、最终报告落库及对账仍须实际验收，不能由组件或模拟报告替代。
- 未经新的明确授权，不推送、不部署。历史记录中的旧门槛不代表当前要求。


本文件是本项目所有开发助手、脚本和人工操作的必读规则。每次开始修改、合并、推送、部署前，都必须重新读取本文件，不得依赖记忆。

## 三条最高优先级红线

1. **未经王总在当前任务中明确同意，禁止部署或重启生产服务。**“执行修复”“继续处理”“测试一下”不等于同意部署；必须出现明确的“同意部署/可以上线”等授权。
2. **未经王总在当前任务中明确同意，禁止合并或直接推送到 `main`。**允许在功能分支本地修改、测试和提交，但不能据此推定有权操作 `main`。
3. **生产只能部署 Git 中已经进入远程 `main` 的明确提交。**禁止从工作区、功能分支、未提交文件、服务器临时修改或其他 ref 部署；禁止 SCP/rsync 绕过 Git 上线。

## 固定执行顺序

### 强制本地优先门禁（王总 2026-09-13 明确要求）

- 每次修复/新方案先列出范围和必测指标，在隔离本地完成适用行为验收和构建，必测项全部通过后才能申请上线；禁止先上线调试。
- 招聘入口/录音/保存变更须验证正常和异常路径；跨服务变更须从同版官网副本投递到独立 HR/Aural 数据和报告对账。本任务手机兼容性采用本地模拟，取消目标真机门槛。纯文档变更只检查文档一致性。
- 模拟组件、完整页面、真实本地后端、真实模型、真机分别记账。任一必测项失败、阻塞、跳过、缺依赖或缺证据，必须报告“本地验收未完成，禁止上线”。自动审批受阻不允许转去生产调试。
- 保留失败和修复历史，不能靠重复重跑隐藏未查明的失败。记录各组件源码版本/内容哈希、依赖与构建、无秘密配置指纹、命令、退出码、日志与隔离证据；代码、配置、依赖、数据库结构、构建或合并结果变化后重跑受影响检查。
- 隔离浏览器之外，还须隔离数据库、文件、队列、worker、模型额度、通知、回调、重试/补偿任务和中转。禁止加载生产 dotenv/凭据，检查默认地址和重定向；未明确配置即拒绝启动。
- 本地通过不代替 main/部署独立授权。制品须与已验收内容对应，仍按远程 main 不可变制品规则发布，并保留限定生产验证与回滚门禁。

`规则与代码审计 -> 可执行正常/异常路径测试 -> 干净生产构建 -> 报告本地验收结果 -> 王总明确同意合并 main -> 合并并推送 main -> 王总明确同意部署 -> 从远程 main 的指定提交部署 -> 版本/服务/API/浏览器验收 -> 另行获批的真实简历（原件或已有脱敏副本）生产模拟面试 -> HR/Aural 数据与报告对账 -> 本次范围内的多样本和并发验收`

合并授权和部署授权是两个独立授权。只同意合并，不代表同意部署；只说修复，也不代表两者中的任何一个。

## Hook 授权参数

得到明确授权后，操作者仍须为 hook 提供与本次动作匹配的参数：

- 合并到 main：`OPRUN_USER_APPROVED=YES` 且 `OPRUN_APPROVED_ACTION=merge-main`
- 推送 main：`OPRUN_USER_APPROVED=YES` 且 `OPRUN_APPROVED_SHA` 必须等于待推送提交 SHA
- 服务端接收 main：推送必须携带 `user-approved=<待推送提交SHA>` push option

这些参数只是技术门禁，不能代替王总的真实授权。没有当前任务中的明确同意，任何人或 AI 都不得自行设置参数绕过门禁。

## 模拟检测边界

本地单元测试、编译、静态检查、使用合成数据调用无写入分析接口属于检测，不构成部署。涉及真实候选人数据写入、服务重启、服务器代码替换、数据库迁移、远程 main 更新的操作均不属于检测，必须单独获得明确同意。

## 招聘发布验收铁律

- 静态源码检查、自动化行为测试、构建、部署健康检查、生产真人面试、多样本和并发分别记账；
  同一种检查重复多次不得冒充不同层级。
- 自动化必须覆盖正常路径及静默、无回答推进、提前完成、抢话/文字竞态、断线重连、主备
  中转、动态扩题、保存同步失败、八题完整性、追问预算和自动音视频入口。
- 生产验收必须从官网真实投递开始，完成语音面试后核对 HR 与 Aural 的八题回答、追问数、
  完成状态、四维评分和证据引用；生产批次、岗位、所用材料和样本数量仍须获得明确授权。
- 2026-09-06 王总单项批准：模拟测试不强制脱敏，可用获授权原件或已有脱敏副本，
  不得因未脱敏阻塞或重复要求制作副本。日志与报告仍保护个人信息；模拟回答标为测试，
  不冒充原简历持有人陈述或能力评价，不向本人发送测试通知。
- 健康检查通过只能报告“已部署，尚未完成生产验收”；单样本通过只能报告“单样本生产验收
  通过”。只有当前范围内所有门槛通过才可报告“全部完成”。
- 代码、配置、制品或部署 SHA 变化后，受影响的下游结果一律作废；任一失败必须修复并从
  受影响层重新验收，禁止复用旧通过结果。
