## 2026-09-15 本次发布授权与例外（仅本次）

用户在获知完整页面至真实模型、最终报告落库及对账尚未完成后，明确回复同意并完全授权执行部署。本次授权涵盖当前入口媒体修复、报告保存确认修复所需的提交、合并 main、推送和不可变制品部署，无须重复询问。

本次豁免未完成的完整报告链路上线前门槛，不能记作该链路通过；后续修改仍执行本地优先要求。并发仅 10 路，iPhone 猎聘真机不作为门槛。生产仅做限定版本、服务、接口与公开页面检查，不向候选人发送测试通知。部署结果另行记录。
# Aural 开发与部署强制入口

## 当前任务验收范围（王总 2026-09-15 更正，优先于本文旧范围）

- 并发验收仅为 10 路，不运行或要求 20 路；历史 20 路失败保留为历史，不再作为上线阻塞。
- 取消 iPhone 猎聘真机验收及上线门槛，保留本地手机模拟，不得冒称真机通过。
- 完整页面到真实模型、最终报告落库及对账仍须实际验收，不能由组件或模拟报告替代。
- 未经新的明确授权，不推送、不部署。历史记录中的旧门槛不代表当前要求。


## 本地优先硬要求（王总 2026-09-13）

每次修复后，先完成隔离本地适用的行为测试与构建，再申请上线。必测项失败、阻塞、跳过或缺证据一律禁止上线，不得借生产调试。模拟测试、完整页面、真实本地服务、模型与真机分别验收；变更后旧证据按影响范围失效。完整规则见 `DEPLOYMENT_POLICY.md` 的本地优先门禁。

每次开始工作，以及任何合并、推送、构建生产包、重启服务或部署前，必须重新读取 `DEPLOYMENT_POLICY.md`。

未经王总在当前任务中明确同意：

- 禁止合并或推送 `main`；
- 禁止部署、替换生产构建或重启 `aural.service`；
- 禁止设置任何批准环境变量绕过 hook。

生产部署前必须先执行 `.githooks/pre-deploy`。只有干净的 `main`、已同步到 `origin/main` 的明确提交可以部署。

## 招聘候选人进入流程铁律

所有标题匹配 `数君招聘 · ...` 的候选人面试必须固定为：

`阅读须知 -> 候选人点击一次“开始面试” -> 系统自动连接摄像头和麦克风 -> AI 自动开场并进入第 1 题`

- 摄像头和麦克风都必须保留并自动连接，不得关闭摄像头。
- 不得插入摄像头测试、麦克风/语音测试、设备检测页或第二个站内开始/授权按钮。
- 浏览器自身首次权限确认不属于站内测试步骤，系统不得试图绕过浏览器安全权限。
- 未经王总在当前任务中明确同意并同步更新回归测试，不得改变此流程。

## 招聘快速开始与真人节奏铁律

- Q1 计分自我介绍、确定性的简历与岗位双锚定 Q2 和有效邀请就绪后必须立即开放面试，
  不得等待模型生成 Q3-Q8。
- Q3-Q8 在候选人阅读须知、连接设备和回答 Q1/Q2 时动态补齐；不得用通用题或虚构简历事实凑数。
- 八道计分题必须全部完成。招聘候选人活跃回答时不得因 32 分钟或其他业务时限强制完成、截断、跳题或关闭语音。
- 正常为 0-2 次就地核验，八题后可有 0-1 次最终动态核验，全场最多 3 次且不要求用满。
- 完整冻结合同见 `docs/OPRUN_RECRUITMENT_INTERVIEW_CONTRACT.md`。未经王总对具体条目的单独手动批准并同步更新测试，不得删除、放宽、替换或重新解释任何条目。

## 招聘发布验收与结论表述铁律

- 规则/源码审计、可执行异常路径测试、干净生产构建、指定 SHA 部署验收、单份获授权真实简历
  生产真人面试、5-10 份质量验收和 10 路并发验收是不同证据层，禁止互相冒充。
- 2026-09-06 王总单项批准：模拟面试可用授权原件或已有脱敏副本，不强制脱敏，
  不因未脱敏重复要求批准或制作副本。保留限量生产批次授权、个人信息保护和测试标识，
  模拟回答不得视为简历本人的陈述或能力评价。
- 重复做三遍静态或正则检查不算三层验收；源码里出现某条规则不等于运行时已经证明。
- 正常路径与静默、无回答推进、提前完成、抢话/文字抢跑、断线重连、主备中转、动态扩题、
  保存同步失败、八题完整性、追问预算和音视频自动连接必须纳入可执行回归。
- 未完成生产真人样本前只能说“本地验收通过”或“已部署但尚未完成生产验收”；单样本通过
  不能冒充多样本或并发通过。只有当前范围全部验收通过才可说“全部完成”。
- 任何代码、配置、制品或部署 SHA 变化都会使受影响的下游验收失效；任何线上失败都必须
  重新打开任务，修复并重跑所有受影响层级，禁止沿用旧结果或称为“完成后的优化”。
